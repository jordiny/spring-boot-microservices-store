package com.jordinymontanez.store.shopping.client;

import com.jordinymontanez.store.shopping.model.Category;
import com.jordinymontanez.store.shopping.model.Product;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Component;
/*
@Component
public class ProductHystrixFallbackFactory implements ProductClient {
    @Override
    public ResponseEntity<Product> getProduct(Long id) {
        Product product=Product.builder()
                .name("none")
                .description("none")
                .category(Category.builder().name("none").build()).build();

        return ResponseEntity.ok(product);
    }

    @Override
    public ResponseEntity<Product> updateStockProduct(Long id, Double quantity) {
        Product product=Product.builder()
                .name("none")
                .description("none")
                .category(Category.builder().name("none").build()).build();

        return ResponseEntity.ok(product);
    }
}*/
